import { useAppDispatch, useAppSelector } from '../../../app/store'
import { addDeckTC } from '../decks-thunks'

// export const useAddDeck = (name: string) => {
//   const dispatch = useAppDispatch()
//   dispatch(addDeckTC(name))
// }
